---
frr_defaults:
  daemons:
    zebra:
      enabled: no
      options: " -s 90000000 --daemon -A 127.0.0.1"
    bgpd:
      enabled: no
      options: "   --daemon -A 127.0.0.1"
    ospfd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    ospf6d:
      enabled: no
      options: " --daemon -A ::1"
    ripd:
      enabled: no
      options: "   --daemon -A 127.0.0.1"
    ripngd:
      enabled: no
      options: " --daemon -A ::1"
    isisd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    pimd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    ldpd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    nhrpd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    eigrpd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    babeld:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    sharpd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    staticd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    pbrd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    bfdd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
    fabricd:
      enabled: no
      options: "  --daemon -A 127.0.0.1"
  vtysh:
      enabled: yes
      # integrated_config: yes
  settings:
    # hostname: 

# FRR configuration settings
# These are combined with frr_defaults in `tasks/main.yml`
frr: {}

# Whether to reload FRR configuration
frr_restart: yes

# Whether to enable system service
frr_enable: yes

# Whether to install FRR
frr_install: yes

# Whether to clean unmanaged configuration
frr_clean_configuration: yes
